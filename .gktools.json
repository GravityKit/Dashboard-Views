{
	"build": ["npm install", "grunt", "composer install --no-dev"],
	"package": {
		"prefix": "gravityview-dashboard",
		"includeExtra": ["vendor", "build"]
	},
	"release": {
		"branch": "main",
		"mergeTo": "develop"
	}
}
